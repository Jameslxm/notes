package com.lixm.notes.guideimport android.os.Bundleimport androidx.appcompat.app.AppCompatActivityimport com.lixm.notes.Rclass GuideActivity : AppCompatActivity() {    override fun onCreate(savedInstanceState: Bundle?) {        super.onCreate(savedInstanceState)        setContentView(R.layout.activity_guide)        guide()    }    private fun guide() {//        val map: MutableMap<View?, String?> =//            LinkedHashMap() //定义需要高亮引导的view集合和对应的提示文字，按顺序加入//        map[btn] = "这是个人信息"//        map[v2] = "这是钱包"//        map[v3] = "这是设置"//        map[v4] = "点这里查看"//        map[v5] = "点这里退出"//        val igv = InnerGuideView.Builder()//            .setColor(Color.argb(180, 0, 0, 0)) //背景色(默认灰色透明)//            .setContext(this@GuideActivity) //传入Activity的context，fragment中使用getActivity()即可//            .setViewsMap(map) //需要高亮的view集合//            .setBorderType(InnerGuideView.BorderType.ROUNDRECT) //高亮view的形状//            .setRoundRadius(10) //圆角矩形的弧度//            .setTipTextColor(Color.RED) //提示文字的颜色//            .setDash(true) //是否显示虚线边框//            .build()//        igv.show()    }}