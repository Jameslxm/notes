package com.lixm.notes.kotlin.commissionimport org.junit.Testclass CommissionTest {    @Test    fun testCommission(){//        var b = BaseImpl(10)//        var derived = Derived(b)//        derived.print()//        println(derived.message)        val e = Example()        e.p = "new"        println(e.p)        println(e.lazyValue)//第一次调用 get() 会执行已传递给 lazy() 的 lambda 表达式并记录结果        println(e.lazyValue)//只是返回记录的结果        e.name = "lxm"        e.name = "james"    }    @Test    fun testCommission1(){        var user = User(            mutableMapOf(            "name" to "lxm",            "age" to 16        )        )        println(user.name)        println(user.age)    }}